{
  "name": "file-uploader",
  "version": "1.0.0",
  "description": "Password protected file upload/download website with AWS S3",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "server": "node server.js",
    "client": "cd client && npm run dev",
    "dev": "node server.js",
    "setup": "npm install && cd client && npm install && cd ..",
    "build:client": "cd client && npm run build && cd ..",
    "deploy:local": "npm run build:client && mkdir -p public && cp -r client/dist/* public/",
    "vercel:dev": "vercel dev",
    "vercel:deploy": "vercel",
    "vercel:prod": "vercel --prod"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.450.0",
    "@aws-sdk/s3-request-presigner": "^3.450.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "mongoose": "^8.0.0",
    "multer": "^1.4.5-lts.1",
    "multer-s3": "^3.0.1",
    "uuid": "^9.0.1"
  }
}